[project]
name = "mdl-collector"
version = "0.1.0"
description = "Collect UNHCR and World Bank microdata catalog metadata"
readme = "README.md"
requires-python = ">=3.10,<4.0"
dependencies = [
    "pandas>=2.2.2,<3.0.0",
    "requests>=2.31.0,<3.0.0",
]

[tool.uv]
package = false
